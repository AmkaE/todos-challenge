// array that holds the uncompeleted todos
export const todos = [];
// array that holds the completed todos
export const completedTodos = [];

// array that holds the list items (li's) based on the completed todos array's length
export const listItems = [];
// array that holds the list items (li's) based on the uncompleted todos array's length
export const completedListItems = [];
